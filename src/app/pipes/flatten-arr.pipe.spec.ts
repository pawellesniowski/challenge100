import { FlattenArrPipe } from './flatten-arr.pipe';

describe('FlattenArrPipe', () => {
  it('create an instance', () => {
    const pipe = new FlattenArrPipe();
    expect(pipe).toBeTruthy();
  });
});
